[phases.install]
cmd = "npm install"

[phases.build]
cmds = [
  "npx prisma generate --schema src/prisma/schema.prisma",
  "npm run build"
]

[start]
cmd = "npx prisma generate --schema src/prisma/schema.prisma && npm run start"
